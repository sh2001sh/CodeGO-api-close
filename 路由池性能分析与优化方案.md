# New API 路由池性能分析与优化方案

> 基于生产数据库分析 - 2026/09/03

---

## 📊 当前系统状态

### 渠道配置
- **总渠道数**: 156 个
- **活跃渠道**: 148 个
- **路由池**: 3 个（1个禁用，2个启用）

### 路由池配置

| 路由池 | 分组 | 状态 | 成员数 | 成员渠道 |
|--------|------|------|--------|----------|
| Plus分组 自动路由 | Plus分组 | ❌ **已禁用** | 4 | tn(0.12x), ling(0.81x), image-niu(1x), image-tp(1x) |
| CC-kiro 自动路由 | CC-kiro | ✅ 启用 | 2 | kiro(0.31x), kiro-ling(0.31x) |
| CC-Max 自动路由 | CC-Max | ✅ 启用 | 2 | ling-claude(1x), max(1x) |

---

## 🔴 核心问题分析

### 问题 1: 极端慢速渠道拖累整体性能

#### 最慢的渠道（TTFT > 25秒）

| 渠道ID | 渠道名 | 分组 | 平均TTFT | 请求量 | 成功率 |
|--------|--------|------|----------|--------|--------|
| 10 | **free** | free | **116秒** | 11,781 | 65.41% |
| 151 | 188-Codex Pro-0.188x | Codex-Pro-bb5fbe | **99秒** | 501 | 83.63% |
| 90 | 91-Codex Pro-0.24x | Codex-Pro-620493 | **57秒** | 258 | 70.93% |
| 65 | 58-Codex Plus-0.08x | Codex-Plus-936beb | **52秒** | 310 | 23.55% |
| 184 | 200-Codex Plus-0.08x | Codex-Plus-f4f552 | **49秒** | 99 | 74.75% |
| 70 | 41-Codex Plus-0.13x | Codex-Plus-1731f7 | **48秒** | 97 | 14.43% |
| 143 | 153-Codex Pro-0.12x | Codex-Pro-7ef2f4 | **42秒** | 7,257 | 95.60% |
| 173 | 187-Codex Pro-0.2x | Codex-Pro-ed9429 | **41秒** | 242 | 95.45% |
| 77 | 73-Codex Pro-0.09x | Codex-Pro-cbeb94 | **39秒** | 6,047 | 89.15% |
| 66 | 59-Codex Plus-0.12x | Codex-Plus-dec7a4 | **37秒** | 6,040 | 79.82% |

**关键发现**：
- 🚨 **free 渠道** TTFT 高达 116秒，却有 11,781 次请求
- 🚨 多个 Codex Pro/Plus 渠道 TTFT 在 40-100秒范围
- 🚨 部分慢速渠道成功率也很低（<25%），双重劣化

### 问题 2: Plus分组路由池被禁用

**Plus分组 自动路由** 是最大的路由池（包含主力渠道 ling 和 tn），但**当前已禁用**。

#### 受影响的渠道性能：

| 渠道 | TTFT | 请求量 | 成功率 | 在路由池中的成本 |
|------|------|--------|--------|-----------------|
| tn | 19.2秒 | 952 | - | **0.12x** (最低) |
| ling | 11.0秒 | 472,134 | 91.28% | 0.81x |
| image-niu | - | - | - | 1x |
| image-tp | 33.3秒 | 564 | 66.84% | 1x |

**问题**：
- tn 渠道成本最低(0.12x)且性能尚可(19.2秒)，但因路由池禁用无法智能分配
- ling 渠道承载了 47万+ 请求，负载极高
- 路由池禁用导致无法根据性能动态选择

### 问题 3: 每个分组只有1个渠道（无负载均衡）

数据显示：148个活跃渠道分布在**148个不同的分组**，意味着：
- ❌ 每个分组只有1个渠道，无法实现负载均衡
- ❌ 渠道故障时无备份，直接失败
- ❌ 慢速渠道无替代，用户被迫等待

### 问题 4: 高负载渠道缺乏分流

| 渠道 | 请求量 | 占比 |
|------|--------|------|
| ling (Plus分组) | 472,134 | 45% |
| 自建pro (纯Pro号池) | 460,645 | 44% |
| 其他146个渠道 | 113,221 | 11% |

**两个渠道承载了89%的流量**，存在单点风险。

---

## ⚡ 优化方案

### 方案 A: 立即优化（0成本，立竿见影）

#### A1. 启用 Plus分组 自动路由池

**操作**：在管理后台启用 "Plus分组 自动路由"

**预期效果**：
- ✅ 系统自动根据 TTFT、成功率、成本选择最优渠道
- ✅ tn (0.12x成本) 在低峰时段自动承接更多流量
- ✅ ling 负载降低 20-30%
- ✅ 首字延迟降低 15-25%

**配置建议**（如果支持权重调整）：
```yaml
multiplier_weight: 20   # 成本权重（降低）
ttft_weight: 50         # TTFT权重（提高）
success_weight: 20      # 成功率权重
cache_weight: 10        # 缓存权重
```

#### A2. 隔离/禁用极慢渠道

**建议禁用的渠道**：

| 渠道ID | 渠道名 | 原因 | 操作 |
|--------|--------|------|------|
| 10 | free | TTFT 116秒，用户体验极差 | ❌ 禁用 |
| 151 | 188-Codex Pro-0.188x | TTFT 99秒 | ❌ 禁用 |
| 90 | 91-Codex Pro-0.24x | TTFT 57秒 | ❌ 禁用 |
| 65 | 58-Codex Plus-0.08x | TTFT 52秒 + 成功率23% | ❌ 禁用 |
| 70 | 41-Codex Plus-0.13x | TTFT 48秒 + 成功率14% | ❌ 禁用 |

**或**：将它们放入独立的"低速池"分组，供对延迟不敏感的场景使用。

#### A3. 合并分组实现真正的负载均衡

**当前问题**：每个分组只有1个渠道
**解决方案**：按成本和性能层级合并分组

**推荐分组策略**：

```
🔵 Ultra-Fast 分组（TTFT < 8秒）
- 1-Codex Pro-0.17x (TTFT 4.1秒)
- 139-Codex Pro-0.18x (TTFT 4.9秒)
- 213-CC-Kiro-0.12x (TTFT 5.8秒)
- 112-CC-Max-0.05x (TTFT 6.1秒)
- 113-CC其它-0.45x (TTFT 6.5秒)

🟢 Fast 分组（TTFT 8-15秒）
- ling, 自建pro, deepseek, 66-国产模型
- 8-国产模型, 36-CC-Kiro, 67-Codex Pro
- 61-Codex Pro, 190-Codex Pro 等

🟡 Standard 分组（TTFT 15-30秒）
- 大部分 Codex Plus/Pro 渠道

🔴 Slow 分组（TTFT > 30秒）
- 仅供非实时场景使用
```

**操作步骤**：
1. 在管理后台批量修改渠道的 `group` 字段
2. 为每个新分组创建路由池
3. 启用路由池自动选择

---

### 方案 B: 中期优化（需要开发，效果显著）

#### B1. 实现自适应TTFT惩罚

**当前代码已支持**（位于 `route_pool_scoring.go`），但需要确认配置：

```go
// 代码中已实现的逻辑
- TTFT > 中位数 2.5倍 → 权重 x2（严重惩罚）
- TTFT > 中位数 1.5倍 → 权重 x1.35（中等惩罚）
```

**验证方法**：检查路由池的 `ttft_weight` 是否生效。

#### B2. 实现熔断机制

**当前代码已支持**（位于 `channel_health.go`），确保启用：

```go
// 自动熔断条件
- 连续失败 >= 3次 → 进入 cooling 状态
- 成功率 < 85% (10+ 请求) → 触发降级
- TTFT > 2.5倍中位数 → 触发迁移
```

**检查**：查看 Redis/内存中的 `channel_health` 缓存是否正常工作。

#### B3. 实现粘性路由（Affinity）

**代码已实现**（`route_pool_affinity.go`），3分钟内同一用户请求会优先使用上次成功的渠道。

**验证**：检查是否启用了 `routePoolAffinityTTL`。

---

### 方案 C: 关于"竞速模式"

你提到的"多分组并发请求，先回复就断其他"：

#### ⚠️ 成本分析

| 场景 | 并发数 | 成本倍数 | 每月额外成本（假设月费$1000） |
|------|--------|----------|-------------------------------|
| 当前单次请求 | 1 | 1x | $0 |
| 2路竞速 | 2 | **1.8-2x** | +$800-$1000 |
| 3路竞速 | 3 | **2.7-3x** | +$1700-$2000 |

**为什么不是整数倍？**
- 先完成的请求会尝试取消其他请求
- 但大部分上游 API 已开始计费（首字节返回后）

#### 📉 断开后的计费情况

| 上游提供商 | 取消后是否计费 | 说明 |
|-----------|---------------|------|
| OpenAI | ✅ **仍计费** | 已生成的 token 全额计费 |
| Anthropic (Claude) | ✅ **仍计费** | 流式开始后的 token 计费 |
| 国内中转商 | ✅ **仍计费** | 大部分按请求计费，不支持取消 |
| 自建 API | ⚠️ 取决于实现 | 需要自己实现取消逻辑 |

#### 💡 更优的替代方案

**不使用竞速，而是：**

1. **智能路由池** + **TTFT权重优化**（方案A + B）
   - 成本：0
   - 效果：首字延迟降低 30-50%

2. **预热机制**（如果支持）
   - 高频用户的首次请求使用快速小模型预热连接
   - 后续请求复用连接，TTFT 降低 50-70%

3. **分层服务**
   - VIP 用户 → Ultra-Fast 分组
   - 普通用户 → Fast/Standard 分组
   - 低价用户 → Slow 分组

#### 🎯 如果仍要实现竞速模式

**推荐配置**：
- 仅对 TTFT > 20秒 的请求触发竞速
- 最多并发 2 路（不要3路+）
- 优先选择不同 fault domain 的渠道（避免同一上游）

**实现伪代码**：
```go
// 伪代码示例
if estimatedTTFT > 20000 {  // 预估超过20秒
    ctx, cancel := context.WithCancel(parentCtx)
    defer cancel()
    
    results := make(chan Response, 2)
    
    // 并发请求两个最快的候选渠道
    go requestChannel(ctx, fastestChannel, results)
    go requestChannel(ctx, secondFastestChannel, results)
    
    // 谁先返回用谁
    firstResponse := <-results
    cancel()  // 取消另一个请求（但可能已在计费）
    
    return firstResponse
}
```

**预期成本增加**：
- 如果只对20%的慢请求启用竞速：+20-40% 成本
- 如果对所有请求启用：+80-100% 成本

---

## 📈 优化效果预估

### 方案 A 实施后（立即可做）

| 指标 | 当前 | 优化后 | 改善 |
|------|------|--------|------|
| 平均TTFT | ~15-20秒 | ~10-14秒 | **↓ 30-40%** |
| P95 TTFT | ~40秒+ | ~25秒 | **↓ 37.5%** |
| ling渠道负载 | 472,134 | ~330,000 | **↓ 30%** |
| 慢速请求占比 | ~25% | ~8% | **↓ 68%** |
| 成本 | $0 | $0 | **0元** |

### 方案 B 实施后（需要开发）

| 指标 | 优化后A | 优化后A+B | 额外改善 |
|------|---------|-----------|----------|
| 平均TTFT | 10-14秒 | 8-12秒 | **↓ 15-20%** |
| 慢速渠道自动熔断 | ❌ | ✅ | **0故障容忍** |
| 用户粘性体验 | 一般 | 优秀 | **一致性体验** |

### 方案 C 实施后（竞速模式）

| 指标 | 优化后A+B | 优化后A+B+C | 额外改善 | 成本 |
|------|-----------|-------------|----------|------|
| 首字延迟 | 8-12秒 | 6-10秒 | **↓ 20-25%** | **+50-100%** 💰 |

---

## 🎯 推荐执行计划

### Phase 1: 立即执行（今天）

1. ✅ **启用 Plus分组 自动路由池**
2. ✅ **禁用 free 渠道**（或移到独立分组）
3. ✅ **禁用 TTFT > 50秒 的渠道**

**预期时间**: 10分钟  
**预期效果**: 首字延迟立即降低 30%+

### Phase 2: 本周内

1. ✅ **合并渠道分组**（按 TTFT 层级）
2. ✅ **为新分组创建路由池**
3. ✅ **配置路由池权重**（提高 TTFT 权重到 50%）

**预期时间**: 1-2小时  
**预期效果**: 首字延迟再降低 15-20%

### Phase 3: 本月内（可选）

1. ⚠️ **验证熔断机制是否生效**
2. ⚠️ **验证粘性路由是否启用**
3. ⚠️ **监控路由池健康度指标**

**预期时间**: 半天  
**预期效果**: 系统稳定性提升

### Phase 4: 未来考虑（不推荐）

❌ **竞速模式** - 成本过高，不推荐
✅ **替代方案**: 继续优化路由池权重和分组策略

---

## 📋 监控指标

实施后需要监控的关键指标：

```sql
-- 1. 各分组的平均 TTFT
SELECT 
    c."group",
    ROUND(AVG(CASE WHEN cpm.ttft_count > 0 
        THEN cpm.ttft_sum_ms::numeric / cpm.ttft_count END)) as avg_ttft,
    SUM(cpm.request_count) as requests
FROM channels c
JOIN channel_perf_metrics cpm ON c.id = cpm.channel_id
WHERE c.status = 1 
    AND cpm.bucket_ts > EXTRACT(EPOCH FROM NOW() - INTERVAL '1 hour') * 1000
GROUP BY c."group"
ORDER BY avg_ttft DESC;

-- 2. 路由池选择分布
SELECT 
    c.name,
    COUNT(*) as selections,
    AVG(成本) as avg_cost
FROM route_pool_selections rps
JOIN channels c ON rps.channel_id = c.id
WHERE rps.created_at > NOW() - INTERVAL '1 hour'
GROUP BY c.name
ORDER BY selections DESC;

-- 3. 慢速请求占比
SELECT 
    COUNT(CASE WHEN ttft_ms > 20000 THEN 1 END)::float / 
    NULLIF(COUNT(*), 0) * 100 as slow_request_pct
FROM gateway_request_audits
WHERE started_at > NOW() - INTERVAL '1 hour';
```

---

## 🔧 技术支持

### 路由池配置位置

**前端路径**: 管理后台 → 渠道管理 → 路由池管理  
**API端点**: `/api/marketplace/route-pools`  
**数据库表**: `route_pools`, `route_pool_members`

### 渠道分组批量修改

```sql
-- 示例：将多个渠道合并到 Ultra-Fast 分组
UPDATE channels 
SET "group" = 'Ultra-Fast'
WHERE id IN (18, 134, 195, 109, 110);
```

---

## 📞 总结

**当前核心问题**：
1. ❌ Plus分组路由池被禁用，无法智能选择
2. ❌ 每个分组只有1个渠道，无负载均衡
3. ❌ 极慢渠道（116秒、99秒、57秒）仍在服务用户
4. ❌ 两个渠道承载89%流量，存在单点风险

**推荐方案**：
1. ✅ **立即启用 Plus分组路由池**（0成本，立竿见影）
2. ✅ **禁用/隔离极慢渠道**（提升用户体验）
3. ✅ **合并分组实现负载均衡**（本周内完成）
4. ❌ **不推荐竞速模式**（成本太高，收益有限）

**预期效果**：
- 首字延迟降低 **40-50%**
- 慢速请求占比从 25% 降至 **8%以下**
- 主力渠道负载降低 **30%**
- **0 额外成本**

---

**生成时间**: 2026-09-03  
**数据来源**: codego-new-db PostgreSQL 数据库  
**分析工具**: Claude Code
